
- 
  - apt
  - command-not-found
  - cowsay
  - ctags
  - curl
  - git
  - man
  - python3
  - silversearcher-ag
  - ssh
  - strace
  - tig
  - tmux
  - vim
  - wget
  - python3-pip

-
  - layer

-
  - pip
  - isort
  - autopep8
  # - pre-commit
  - thefuck

- [layer]
# install fzf
- 
  - eval
  - wget https://github.com/junegunn/fzf-bin/releases/download/0.16.7/fzf-0.16.7-linux_amd64.tgz -O /tmp/fzf.tgz
  - tar -xzf /tmp/fzf.tgz -C /tmp
  - cp /tmp/fzf /usr/local/bin/
  - rm /tmp/fzf /tmp/fzf.tgz
- [layer]
# install docker
- [import-prefixed-envs, HOME, USER, PATH]
- 
  - eval
  - ln -s $HOST_HOME/.ssh/ /root/ || true
  - echo "ssh -q -t $HOST_USER@192.168.65.1 \"cd \$PWD && PATH=$HOST_PATH \$@\"" > /usr/local/bin/hostcmd
  - chmod u+x /usr/local/bin/hostcmd

- [layer]

- 
  - eval
  - echo 'hostcmd plash $@'  > /usr/local/bin/plash
  - chmod u+x /usr/local/bin/plash


  - echo 'hostcmd docker $@'  > /usr/local/bin/docker
  - chmod u+x /usr/local/bin/docker

- [layer]

- [layer]

- 
  - eval
  - curl -L https://github.com/docker/compose/releases/download/1.12.0/docker-compose-`uname -s`-`uname -m` > /usr/local/bin/docker-compose
  - chmod u+x /usr/local/bin/docker-compose

- [layer]
